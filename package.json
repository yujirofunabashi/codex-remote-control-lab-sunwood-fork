{
  "name": "codex-remote-control-lab",
  "version": "1.0.0",
  "description": "Local lab for OpenAI Codex CLI remote-control and app-server experiments.",
  "private": false,
  "type": "commonjs",
  "scripts": {
    "version:codex": "codex --version",
    "help:remote-control": "codex remote-control --help",
    "server:ws": "mkdir -p .codex-home-ws && CODEX_HOME=$PWD/.codex-home-ws codex app-server --listen ws://127.0.0.1:45213",
    "probe:ws": "node scripts/probe-ws.js",
    "phone": "node scripts/start-phone.js",
    "phone:fleet": "node scripts/start-fleet.js",
    "phone:claude": "PHONE_AGENT_PROVIDER_DEFAULT=claude node scripts/start-phone.js",
    "screenshots:readme": "node scripts/capture-readme-screenshots.js",
    "smoke:mobile": "node scripts/mobile-smoke.js",
    "phone:loop": "sh -c 'while :; do PHONE_SUPERVISED=1 npm run phone; code=$?; [ \"$code\" = 42 ] || exit \"$code\"; sleep 1; done'",
    "test": "node --test scripts/*.test.js",
    "check": "node --check scripts/start-phone.js && node --check scripts/claude-approval-mcp.js && node --check scripts/remote-url.js && node --check scripts/claude-sessions.js && node --check scripts/start-fleet.js && node --check scripts/probe-ws.js && node --check scripts/read-desktop-rate-limits.js && node --check scripts/capture-claude-rate-limits.js && node --check scripts/mobile-smoke.js && node --check public/phone-ui-utils.js && node --check public/main.js",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "url:remote": "node scripts/remote-url.js",
    "phone:loop:claude": "PHONE_AGENT_PROVIDER_DEFAULT=claude npm run phone:loop",
    "sessions": "node scripts/claude-sessions.js"
  },
  "keywords": [
    "codex",
    "remote-control",
    "app-server",
    "websocket",
    "phone-bridge",
    "local-first"
  ],
  "author": "Sunwood AI Labs",
  "license": "ISC",
  "devDependencies": {
    "@openai/codex": "^0.146.0",
    "playwright": "^1.59.1",
    "vitepress": "^1.6.4",
    "ws": "^8.20.0"
  }
}
